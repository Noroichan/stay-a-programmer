spring:
  application:
    name: 'stay-a-programmer'

  servlet:
    context-path: '/api'

  datasource:
    url: 'jdbc:postgresql://localhost:5432/shop_db'
    username: root
    password: LetMeIn
    driver-class: org.postgresql.Driver

  redis:
    host: localhost
    port: 6397
    password: LetMeIn

config:
  cookie:
    max-age: 86400
