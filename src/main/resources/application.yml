spring:
  application:
    name: 'stay-a-programmer'

  datasource:
    url: 'jdbc:postgresql://localhost:5432/shop_db'
    username: root
    password: LetMeIn

    hikari:
      connection-timeout: 10000
